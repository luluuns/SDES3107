//Write a program to convert the value of an integer to other data types. Display the 
// conversions in the console.

float f = 65.0;
byte b = byte(f);         
b = byte(c);              
f = 130.0;
b = byte(f);             
